import React from "react";
import styled from "styled-components";

function List({ title, type, el }) {
  return <SContainer>List</SContainer>;
}

export default List;

const SContainer = styled.div``;
